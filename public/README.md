# Public Assets Folder

Place static assets here that you want to be served at the root path (`/`).

## Favicon
You can drop your favicon here (e.g., as `favicon.ico`, `favicon.png`, or `favicon.svg`).
Once placed, they will be accessible at `/favicon.ico` or `/favicon.png` etc.
